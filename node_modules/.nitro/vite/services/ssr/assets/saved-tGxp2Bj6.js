import { C as useSetApplicationStatus, D as useToggleSave, E as useStudentProfile, S as useSaved, T as useSetReminderPrefs, b as useReminderPrefs, d as useApplications, h as useInternships } from "./ai-queries-D3oXUHn6.js";
import { i as scoreInternship, t as daysUntil } from "./matching-Ba8zFjsF.js";
import { n as cn, t as Button } from "./button-BpE9Czok.js";
import { t as Navbar } from "./Navbar-gsSgjcN9.js";
import { t as Card } from "./card-BM637P_-.js";
import { t as InternshipCard } from "./InternshipCard-1qzylkJO.js";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { BellRing, Loader2 } from "lucide-react";
//#region src/routes/_authenticated/saved.tsx?tsr-split=component
var REMINDER_DAYS = [
	7,
	3,
	1
];
function SavedPage() {
	const { data: profile } = useStudentProfile();
	const { data: internships = [], isLoading } = useInternships();
	const { data: saved = [] } = useSaved();
	const { data: reminderDays = [
		7,
		3,
		1
	] } = useReminderPrefs();
	const setReminders = useSetReminderPrefs();
	const toggleSave = useToggleSave();
	const { data: applications = [] } = useApplications();
	const setStatus = useSetApplicationStatus();
	const savedIds = saved.map((s) => s.internship_id);
	const items = internships.filter((i) => savedIds.includes(i.id));
	const dueSoon = items.filter((i) => {
		const d = daysUntil(i.deadline);
		return d !== null && d >= 0 && reminderDays.some((r) => d <= r);
	});
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-background",
		children: [/* @__PURE__ */ jsx(Navbar, {}), /* @__PURE__ */ jsxs("main", {
			className: "mx-auto max-w-6xl px-4 py-10 sm:px-6",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-3xl font-semibold",
					children: "Saved internships"
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-2 text-muted-foreground",
					children: [items.length, " shortlisted. We nudge you before each deadline."]
				}),
				/* @__PURE__ */ jsxs(Card, {
					className: "mt-6 gap-3 p-5 shadow-[var(--shadow-card)]",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-2 font-medium",
							children: [/* @__PURE__ */ jsx(BellRing, { className: "size-4 text-primary" }), " Deadline reminders"]
						}),
						/* @__PURE__ */ jsx("div", {
							className: "flex flex-wrap gap-2",
							children: REMINDER_DAYS.map((d) => {
								const active = reminderDays.includes(d);
								return /* @__PURE__ */ jsxs("button", {
									type: "button",
									onClick: () => setReminders.mutate(active ? reminderDays.filter((x) => x !== d) : [...reminderDays, d].sort((a, b) => b - a)),
									className: cn("rounded-full border px-4 py-1.5 text-sm font-medium transition-colors", active ? "border-primary bg-primary text-primary-foreground" : "border-border bg-card hover:bg-muted"),
									children: [
										d,
										" day",
										d > 1 ? "s" : "",
										" before"
									]
								}, d);
							})
						}),
						dueSoon.length > 0 && /* @__PURE__ */ jsx("ul", {
							className: "mt-2 space-y-1.5 text-sm",
							children: dueSoon.map((i) => {
								const d = daysUntil(i.deadline);
								return /* @__PURE__ */ jsxs("li", {
									className: "rounded-lg bg-accent px-3 py-2 text-accent-foreground",
									children: [
										/* @__PURE__ */ jsx("strong", { children: i.title }),
										" at ",
										i.company_name,
										" closes",
										" ",
										d === 0 ? "today" : `in ${d} day${d > 1 ? "s" : ""}`,
										"."
									]
								}, i.id);
							})
						})
					]
				}),
				isLoading ? /* @__PURE__ */ jsx("div", {
					className: "flex justify-center py-24",
					children: /* @__PURE__ */ jsx(Loader2, { className: "size-6 animate-spin text-muted-foreground" })
				}) : items.length === 0 ? /* @__PURE__ */ jsxs("div", {
					className: "mt-16 text-center",
					children: [/* @__PURE__ */ jsx("p", {
						className: "text-muted-foreground",
						children: "Nothing saved yet."
					}), /* @__PURE__ */ jsx(Button, {
						className: "mt-4",
						asChild: true,
						children: /* @__PURE__ */ jsx(Link, {
							to: "/dashboard",
							children: "Find matches"
						})
					})]
				}) : /* @__PURE__ */ jsx("div", {
					className: "mt-6 grid gap-4 lg:grid-cols-2",
					children: items.map((internship) => /* @__PURE__ */ jsx(InternshipCard, {
						item: profile ? scoreInternship(internship, profile) : {
							internship,
							score: 0,
							matchedSkills: [],
							matchedInterests: [],
							reason: "",
							reasons: []
						},
						saved: true,
						onToggleSave: () => toggleSave.mutate({
							internshipId: internship.id,
							saved: true
						}),
						status: applications.find((a) => a.internship_id === internship.id)?.status,
						onMarkApplied: () => setStatus.mutate({
							internshipId: internship.id,
							status: "applied"
						})
					}, internship.id))
				})
			]
		})]
	});
}
//#endregion
export { SavedPage as component };
