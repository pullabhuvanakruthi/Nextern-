import { _ as useAssistantMessages, p as useStudentProfile, v as useAssistantSend } from "./queries-DoJqRnYK.js";
import { n as useAuth } from "./auth-context-DxBoya-C.js";
import { n as cn, t as Button } from "./button-BpE9Czok.js";
import { t as Textarea } from "./textarea-Cp94w9lz.js";
import { useEffect, useRef, useState } from "react";
import { Navigate, Outlet, useLocation } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { Bot, Loader2, Send, X } from "lucide-react";
//#region src/components/AssistantWidget.tsx
var PROMPTS = [
	"Make my resume more ATS-friendly",
	"Rewrite my project bullets with impact",
	"What skills should I add for AI/ML roles?"
];
function AssistantWidget() {
	const [open, setOpen] = useState(false);
	const [input, setInput] = useState("");
	const { data: messages = [] } = useAssistantMessages();
	const send = useAssistantSend();
	const endRef = useRef(null);
	useEffect(() => {
		endRef.current?.scrollIntoView({ behavior: "smooth" });
	}, [
		messages.length,
		send.isPending,
		open
	]);
	const submit = (text) => {
		const value = text.trim();
		if (!value || send.isPending) return;
		setInput("");
		send.mutate(value, { onError: (e) => toast.error(e instanceof Error ? e.message : "Something went wrong") });
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [!open && /* @__PURE__ */ jsxs(Button, {
		onClick: () => setOpen(true),
		className: "fixed bottom-5 right-5 z-50 h-12 gap-2 rounded-full px-5 shadow-lg",
		children: [/* @__PURE__ */ jsx(Bot, { className: "size-5" }), " AI Assistant"]
	}), open && /* @__PURE__ */ jsxs("div", {
		className: "fixed bottom-5 right-5 z-50 flex h-[32rem] w-[min(24rem,calc(100vw-2.5rem))] flex-col overflow-hidden rounded-2xl border border-border bg-card shadow-2xl",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-center justify-between border-b border-border px-4 py-3",
				children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
					className: "text-sm font-semibold",
					children: "Nextern AI Assistant"
				}), /* @__PURE__ */ jsx("p", {
					className: "text-xs text-muted-foreground",
					children: "Resume & ATS coaching"
				})] }), /* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					size: "icon",
					onClick: () => setOpen(false),
					"aria-label": "Close assistant",
					children: /* @__PURE__ */ jsx(X, { className: "size-4" })
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex-1 space-y-3 overflow-y-auto px-4 py-4",
				children: [
					messages.length === 0 && /* @__PURE__ */ jsxs("div", {
						className: "space-y-3",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm text-muted-foreground",
							children: "Ask me to tighten your resume, fix ATS issues, or prep for an interview."
						}), PROMPTS.map((p) => /* @__PURE__ */ jsx("button", {
							onClick: () => submit(p),
							className: "block w-full rounded-lg border border-border px-3 py-2 text-left text-sm hover:bg-muted",
							children: p
						}, p))]
					}),
					messages.map((m) => /* @__PURE__ */ jsx("div", {
						className: cn("max-w-[85%] whitespace-pre-wrap rounded-xl px-3 py-2 text-sm", m.role === "user" ? "ml-auto bg-primary text-primary-foreground" : "bg-muted text-foreground"),
						children: m.content
					}, m.id)),
					send.isPending && /* @__PURE__ */ jsx("div", {
						className: "w-fit rounded-xl bg-muted px-3 py-2 text-sm text-muted-foreground",
						children: "Thinking…"
					}),
					/* @__PURE__ */ jsx("div", { ref: endRef })
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-end gap-2 border-t border-border p-3",
				children: [/* @__PURE__ */ jsx(Textarea, {
					value: input,
					onChange: (e) => setInput(e.target.value),
					onKeyDown: (e) => {
						if (e.key === "Enter" && !e.shiftKey) {
							e.preventDefault();
							submit(input);
						}
					},
					placeholder: "Ask about your resume…",
					rows: 1,
					className: "max-h-28 min-h-10 resize-none"
				}), /* @__PURE__ */ jsx(Button, {
					size: "icon",
					onClick: () => submit(input),
					disabled: send.isPending,
					"aria-label": "Send",
					children: /* @__PURE__ */ jsx(Send, { className: "size-4" })
				})]
			})
		]
	})] });
}
//#endregion
//#region src/routes/_authenticated/route.tsx?tsr-split=component
/** Students land in the guided profile builder before any match-driven screen. */
function AuthedShell() {
	const { pathname } = useLocation();
	const { role, loading } = useAuth();
	const { data: profile, isLoading } = useStudentProfile();
	const isRecruiter = role === "recruiter";
	const exempt = pathname.startsWith("/onboarding");
	if (!isRecruiter && (loading || isLoading)) return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center",
		children: /* @__PURE__ */ jsx(Loader2, { className: "size-6 animate-spin text-muted-foreground" })
	});
	if (!isRecruiter && !exempt && profile && !profile.onboarding_complete) return /* @__PURE__ */ jsx(Navigate, {
		to: "/onboarding",
		replace: true
	});
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(Outlet, {}), !isRecruiter && !exempt && /* @__PURE__ */ jsx(AssistantWidget, {})] });
}
//#endregion
export { AuthedShell as component };
