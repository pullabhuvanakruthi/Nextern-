import { n as useCandidates, o as useMyInternships } from "./queries-DoJqRnYK.js";
import { t as Route } from "./recruiter._internshipId-BV9WWs9_.js";
import { t as Button } from "./button-BpE9Czok.js";
import { t as Navbar } from "./Navbar-CAX5iZjz.js";
import { t as Card } from "./card-BM637P_-.js";
import { t as MatchScore } from "./MatchScore-CSkniks3.js";
import { t as Badge } from "./badge-DHlcf1ty.js";
import { useMemo } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { ArrowLeft, FileCheck2, Loader2 } from "lucide-react";
//#region src/routes/_authenticated/recruiter.$internshipId.tsx?tsr-split=component
function CandidatesPage() {
	const { internshipId } = Route.useParams();
	const { data: postings = [] } = useMyInternships();
	const { data: candidates = [], isLoading } = useCandidates(internshipId);
	const posting = postings.find((p) => p.id === internshipId);
	const ranked = useMemo(() => {
		const required = (posting?.skills ?? []).map((s) => s.toLowerCase());
		return candidates.map((c) => {
			const matched = (c.skills ?? []).filter((s) => required.includes(s.toLowerCase()));
			const domainFit = (c.interests ?? []).some((i) => i.toLowerCase() === (posting?.domain ?? "").toLowerCase());
			const score = Math.min(99, Math.round(25 + (required.length ? matched.length / required.length * 50 : 0) + (domainFit ? 15 : 0) + (c.has_resume ? 5 : 0) + (c.application_status ? 5 : 0)));
			return {
				...c,
				matched,
				score
			};
		}).sort((a, b) => b.score - a.score);
	}, [candidates, posting]);
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-background",
		children: [/* @__PURE__ */ jsx(Navbar, {}), /* @__PURE__ */ jsxs("main", {
			className: "mx-auto max-w-4xl px-4 py-10 sm:px-6",
			children: [
				/* @__PURE__ */ jsx(Button, {
					variant: "ghost",
					size: "sm",
					asChild: true,
					className: "mb-6",
					children: /* @__PURE__ */ jsxs(Link, {
						to: "/recruiter",
						children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "size-4" }), " Back to postings"]
					})
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "text-3xl font-semibold",
					children: posting?.title ?? "Matched candidates"
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-2 text-muted-foreground",
					children: [ranked.length, " students ranked by suitability. Contact details stay private until a student applies."]
				}),
				isLoading ? /* @__PURE__ */ jsx("div", {
					className: "flex justify-center py-24",
					children: /* @__PURE__ */ jsx(Loader2, { className: "size-6 animate-spin text-muted-foreground" })
				}) : ranked.length === 0 ? /* @__PURE__ */ jsx("p", {
					className: "mt-16 text-center text-muted-foreground",
					children: "No student profiles match this opening yet."
				}) : /* @__PURE__ */ jsx("div", {
					className: "mt-6 space-y-3",
					children: ranked.map((c) => /* @__PURE__ */ jsxs(Card, {
						className: "gap-3 p-5 shadow-[var(--shadow-card)]",
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "flex flex-wrap items-start justify-between gap-3",
								children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
									className: "font-semibold",
									children: c.display_name
								}), /* @__PURE__ */ jsxs("p", {
									className: "text-sm text-muted-foreground",
									children: [[
										c.degree,
										c.specialization,
										c.college
									].filter(Boolean).join(" · ") || "Profile in progress", c.graduation_year ? ` · Class of ${c.graduation_year}` : ""]
								})] }), /* @__PURE__ */ jsxs("div", {
									className: "flex items-center gap-2",
									children: [c.application_status && /* @__PURE__ */ jsx(Badge, {
										variant: "secondary",
										children: c.application_status
									}), /* @__PURE__ */ jsx(MatchScore, { score: c.score })]
								})]
							}),
							/* @__PURE__ */ jsx("div", {
								className: "flex flex-wrap gap-1.5",
								children: (c.skills ?? []).slice(0, 10).map((s) => /* @__PURE__ */ jsx(Badge, {
									variant: c.matched.includes(s) ? "default" : "secondary",
									className: c.matched.includes(s) ? "bg-primary-soft text-accent-foreground" : "",
									children: s
								}, s))
							}),
							c.has_resume && /* @__PURE__ */ jsxs("p", {
								className: "inline-flex items-center gap-1.5 text-xs text-muted-foreground",
								children: [/* @__PURE__ */ jsx(FileCheck2, { className: "size-3.5 text-primary" }), " Resume on file"]
							})
						]
					}, c.user_id))
				})
			]
		})]
	});
}
//#endregion
export { CandidatesPage as component };
