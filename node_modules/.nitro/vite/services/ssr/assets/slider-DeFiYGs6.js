import { k as useUpdateStudentProfile, s as useParseResume } from "./ai-queries-D3oXUHn6.js";
import { t as supabase } from "./client-mGQq5Rj1.js";
import { n as useAuth } from "./auth-context-DxBoya-C.js";
import { n as cn, t as Button } from "./button-BpE9Czok.js";
import * as React from "react";
import { useRef, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { FileText, Loader2, Sparkles, Trash2, Upload } from "lucide-react";
import * as SliderPrimitive from "@radix-ui/react-slider";
//#region src/components/ResumeUpload.tsx
var MAX_BYTES = 5242880;
var ALLOWED = [
	".pdf",
	".doc",
	".docx"
];
function ResumeUpload({ profile }) {
	const { user } = useAuth();
	const update = useUpdateStudentProfile();
	const inputRef = useRef(null);
	const [busy, setBusy] = useState(false);
	const parse = useParseResume();
	const extract = () => {
		parse.mutate(void 0, {
			onSuccess: async (insights) => {
				const merged = Array.from(new Set([...profile.skills ?? [], ...insights.skills ?? []].map((s) => s.trim()).filter(Boolean)));
				await update.mutateAsync({ skills: merged });
				toast.success(`Added ${merged.length - (profile.skills?.length ?? 0)} skills from your resume`);
			},
			onError: (e) => toast.error(e instanceof Error ? e.message : "Could not read your resume")
		});
	};
	const handleFile = async (file) => {
		const ext = file.name.slice(file.name.lastIndexOf(".")).toLowerCase();
		if (!ALLOWED.includes(ext)) {
			toast.error("Upload a PDF, DOC or DOCX file");
			return;
		}
		if (file.size > MAX_BYTES) {
			toast.error("Resume must be under 5 MB");
			return;
		}
		setBusy(true);
		try {
			const path = `${user.id}/resume${ext}`;
			const { error } = await supabase.storage.from("resumes").upload(path, file, { upsert: true });
			if (error) throw error;
			await update.mutateAsync({
				resume_path: path,
				resume_name: file.name,
				resume_uploaded_at: (/* @__PURE__ */ new Date()).toISOString()
			});
			toast.success("Resume uploaded — it now feeds your match scores");
		} catch (error) {
			toast.error(error instanceof Error ? error.message : "Upload failed");
		} finally {
			setBusy(false);
			if (inputRef.current) inputRef.current.value = "";
		}
	};
	const remove = async () => {
		if (!profile.resume_path) return;
		setBusy(true);
		try {
			await supabase.storage.from("resumes").remove([profile.resume_path]);
			await update.mutateAsync({
				resume_path: null,
				resume_name: null,
				resume_uploaded_at: null
			});
			toast.success("Resume removed");
		} finally {
			setBusy(false);
		}
	};
	const download = async () => {
		if (!profile.resume_path) return;
		const { data, error } = await supabase.storage.from("resumes").createSignedUrl(profile.resume_path, 60);
		if (error || !data) {
			toast.error("Could not open resume");
			return;
		}
		window.open(data.signedUrl, "_blank", "noopener");
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "rounded-xl border border-dashed border-border bg-surface p-5",
		children: [/* @__PURE__ */ jsx("input", {
			ref: inputRef,
			type: "file",
			accept: ".pdf,.doc,.docx",
			className: "hidden",
			onChange: (e) => {
				const file = e.target.files?.[0];
				if (file) handleFile(file);
			}
		}), profile.resume_path ? /* @__PURE__ */ jsxs("div", {
			className: "flex flex-wrap items-center justify-between gap-3",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-3",
				children: [/* @__PURE__ */ jsx(FileText, { className: "size-5 text-primary" }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
					className: "text-sm font-medium",
					children: profile.resume_name ?? "Resume"
				}), /* @__PURE__ */ jsxs("p", {
					className: "text-xs text-muted-foreground",
					children: [
						"Uploaded",
						" ",
						profile.resume_uploaded_at ? new Date(profile.resume_uploaded_at).toLocaleDateString() : "recently"
					]
				})] })]
			}), /* @__PURE__ */ jsxs("div", {
				className: "flex gap-2",
				children: [
					/* @__PURE__ */ jsx(Button, {
						variant: "outline",
						size: "sm",
						onClick: download,
						children: "View"
					}),
					/* @__PURE__ */ jsxs(Button, {
						variant: "outline",
						size: "sm",
						onClick: extract,
						disabled: parse.isPending,
						children: [parse.isPending ? /* @__PURE__ */ jsx(Loader2, { className: "size-4 animate-spin" }) : /* @__PURE__ */ jsx(Sparkles, { className: "size-4" }), "Extract skills"]
					}),
					/* @__PURE__ */ jsx(Button, {
						variant: "outline",
						size: "sm",
						onClick: () => inputRef.current?.click(),
						disabled: busy,
						children: "Replace"
					}),
					/* @__PURE__ */ jsx(Button, {
						variant: "ghost",
						size: "icon",
						"aria-label": "Remove resume",
						onClick: remove,
						disabled: busy,
						children: /* @__PURE__ */ jsx(Trash2, { className: "size-4" })
					})
				]
			})]
		}) : /* @__PURE__ */ jsxs("div", {
			className: "flex flex-wrap items-center justify-between gap-3",
			children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
				className: "text-sm font-medium",
				children: "Upload your resume (optional)"
			}), /* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted-foreground",
				children: "PDF, DOC or DOCX up to 5 MB. Stored privately and only visible to you."
			})] }), /* @__PURE__ */ jsxs(Button, {
				onClick: () => inputRef.current?.click(),
				disabled: busy,
				children: [busy ? /* @__PURE__ */ jsx(Loader2, { className: "size-4 animate-spin" }) : /* @__PURE__ */ jsx(Upload, { className: "size-4" }), "Upload resume"]
			})]
		})]
	});
}
//#endregion
//#region src/components/ui/slider.tsx
var Slider = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsxs(SliderPrimitive.Root, {
	ref,
	className: cn("relative flex w-full touch-none select-none items-center", className),
	...props,
	children: [/* @__PURE__ */ jsx(SliderPrimitive.Track, {
		className: "relative h-1.5 w-full grow overflow-hidden rounded-full bg-primary/20",
		children: /* @__PURE__ */ jsx(SliderPrimitive.Range, { className: "absolute h-full bg-primary" })
	}), /* @__PURE__ */ jsx(SliderPrimitive.Thumb, { className: "block h-4 w-4 rounded-full border border-primary/50 bg-background shadow transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50" })]
}));
Slider.displayName = SliderPrimitive.Root.displayName;
//#endregion
export { ResumeUpload as n, Slider as t };
